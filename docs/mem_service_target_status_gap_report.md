# mem_service 独立化状态与缺口闭环报告

更新时间：2026-06-30

## 1. 目标

将 `mem_service` 从当前 guest-side 组件完善为独立服务，满足以下目标：

- 可以独立发布（发布产物可在构建/打包阶段独立产出与校验）
- 可以独立部署（安装布局、部署文件、运行生命周期完整可复用）
- 可以被其他进程通过稳定 API 调用，而非只支持静态链接到 guest app
- 可与 LLM serving 协作（prefix、KV、runtime handoff、execution artifact）
- 可与 pretraining 协作（dataset/sample/checkpoint/gradient/optimizer-state + training-step-commit）
- 具备可运维基础：重启恢复、观测、审计、兼容门禁和升级回滚约束

## 2. 现状（已完成）

### 2.1 已完成的关键能力

- `linqu_mem_service` 的服务进程形态已具备：`serve/health/ready/status/list-records/metrics/audit-log` 等最小 admin 与运行时闭环可用。
- 业务 RPC 与 CLI 已覆盖：object、prefix、KV、runtime handoff、execution artifact、training artifact、training-step-commit。
- query fail-closed 链路已具备上下文校验（`expected_session_id`、`expected_model_key`、`expected_artifact_kind`、`expected_artifact_id`、`expected_owner`、`expected_version`、`expected_checksum`）。
- mutation 可选 idempotency 支持并有 replay/conflict 语义，且结合 `store` 与 journal 可跨重启恢复。
- `audit-log` 与 `metrics` 已有最小实现与导出（含基本直方图、命中/未命中/失败关闭、idempotency 计数）。
- 已支持外部进程调用栈：`mem_service_wire_client` + `mem_service_client`，并有 serving/pretraining 示例覆盖。`installed-sdk-example-smoke` 会从安装后的 public headers、SDK sources、example sources 编译两个外部客户端，证明外部 serving/pretraining 集成不依赖源码树内部路径；安装布局还生成 `lib/pkgconfig/lingqu-mem-service.pc`，通过 `Cflags` 与 `sdk_sources` 暴露 include/source 根路径，避免外部 serving/pretraining 项目硬编码安装路径；`installed-sdk-pkgconfig-smoke` 进一步用 `pkg-config --define-prefix` 读取安装后的 `.pc` 元数据并编译 serving/pretraining 示例，证明该发现契约可被标准外部构建工具实际消费；`installed-sdk-runtime-smoke` 会启动安装后的 host daemon，并用安装后编译出的 serving/pretraining 示例客户端实际连接 Unix socket 跑通 prefix/KV/runtime-handoff/execution-artifact 和 training-artifact/training-step-commit 路径，证明安装后的 daemon 与外部客户端能在运行时协同；`installed_sdk_preflight` 已进入 release/package manifest，用 `scripts/verify_mem_service_installed_sdk.sh --preflight` 让发布系统能在不编译、不启动 daemon 前先机器发现并检查 installed SDK contract。
- 发布/安装能力已打通：release manifest、package manifest、install-smoke、installed-sdk-example-smoke、installed-sdk-runtime-smoke、package-tarball-smoke、package-deb-smoke、package-rpm-smoke（rpm 需要 Linux rpm toolchain）。安装布局已包含 systemd unit 默认引用的 `/etc/lingqu/mem_service/mem_service.conf` runtime config；该 runtime config 来自独立的 `configs/mem_service.runtime.conf`，不再复用 `/tmp` 示例配置。host unit 使用独立的 `configs/mem_service.host.runtime.conf`，安装到 `/etc/lingqu/mem_service/mem_service.host.conf`，使用独立 socket/state/metrics port，避免两个 unit 在真实 systemd 环境争抢同一端口或 socket。enableable unit 安装到 `usr/lib/systemd/system/`；`lingqu-mem-service.pc` 安装到 `usr/lib/pkgconfig/` 并进入 release/package manifest 与 tar/deb/rpm smoke；release/ops/remote-transport 复验脚本安装到 `share/lingqu/mem_service/scripts/`，默认优先使用同一安装前缀下的 `libexec/lingqu/mem_service/linqu_mem_service_host`，找不到才回退源码树 app dir，并进入 package/release manifest 与 tar/deb/rpm smoke，因此发布后的包可以携带自己的证据复验入口；tar/deb/rpm smoke 会验证配置、unit、pkg-config 元数据与复验脚本随包落盘。
- 安装后二进制自描述能力已补齐：`version` 输出稳定 text-kv contract，包含 `service_version`、wire/schema/API ABI/package manifest checksum、release/package manifest 查询命令、`config_security_gate` 与 `version_gate`；`version-fixtures` 已进入 package required gates、release/package manifest、host-artifact-smoke 与 install-smoke。该能力用于独立部署后让运维系统、LLM serving 或 pretraining 侧在不读取源码树的情况下识别当前服务 contract。
- 发布就绪状态自描述能力已补齐：`release-readiness` 输出稳定 text-kv contract，汇总 installed SDK contract、serving/pretraining runtime gate、Linux ops certification、cross-host remote transport certification 和当前 `overall_status`；在外部 Linux/cross-host 证据缺失时保持 `overall_status=not-certified`，并通过 `blocking_external_evidence=linux-ops-certification-bundle,remote-transport-certification-bundle` 明确放行阻塞项。新增 `release-readiness --ops-evidence-file <path> --remote-transport-evidence-file <path>` 后，发布系统可在 bundle 脚本完成安全解包和独立复验后，把 evidence 文件交给 readiness CLI 做最终机器可读放行判定；只有 Linux ops evidence 与 remote transport evidence 都通过同一组 validator 时才输出 `overall_status=certified` 和 `blocking_external_evidence=none`，任一 evidence stale 或 checksum/gate 不匹配都会 fail-closed。readiness report 同时暴露 `release_certification_verify=scripts/verify_mem_service_release_certification.sh --ops-bundle-file --remote-transport-bundle-file`，让只依赖安装后二进制自描述的发布系统也能发现完整 bundle verifier 入口。`release-readiness-fixtures` 已进入 package required gates、release/package manifest 与 install-smoke，覆盖默认 not-certified、证据 certified 与 stale fail-closed，避免发布系统只能从文档推断当前 readiness 状态。
- 生产运维认证边界已显式化：`ops-certification-policy`、`ops-certification-fixtures`、`ops-certification-evidence-fixtures`、`ops-certification-generate-evidence`、`ops-certification-linux-ci-smoke`、`linux-ops-certification-smoke`、`linux-ops-upgrade-rollback-smoke`、`linux-ops-deployment-smoke`、release/package manifest 和 install-smoke 共同把真实 Linux systemd、Prometheus/Alertmanager、rpm、多发布渠道部署升级回滚列为外部硬门禁；当前状态 fail-closed 为 `not-certified`，避免用 simulator smoke 冒充生产认证。
- 合约与门禁体系已具备：
  - wire / wire-schema / store / journal / compat 基础门禁
  - upgrade-rollback / api-abi / admin-output / alert 相关契约
  - `compat-runtime-fixtures` 已落库并对齐安装/发布门禁

### 2.2 最近重要提交

- `d3ab654 Add mem service runtime compatibility fixtures`
  - 增加了 `compat-runtime-fixtures` 运行时兼容门禁
  - 更新兼容性矩阵与发布/安装契约文件
  - 与多项 install-smoke 和 contract 相关测试链路对齐

## 3. 仍存缺口

### 3.1 兼容性完整度

- **已闭环（2026-06-27）**：新增 `compat-old-server-runtime-fixtures` 运行时门禁，通过 per-instance 的 `enforce_expected_context` 开关在同一进程内构造 current server 与 old server 变体，证明 current-client（extended profile）的 `expected_*` 上下文查询在 old server 上被容忍并正常返回数据，而在 current server 上 fail-closed。`new_client_old_server`、`old_server_runtime_binary`、`cross_version_upgrade` 在 release/package/upgrade-rollback/api-abi/compat-baseline/compat-old-new 全部契约中由 `not-certified`/`not-in-tree` 翻转为 `certified`/`in-tree`，并伴随 checksum/len 重算与 Makefile/Python 门禁联动。
- 当前 old/new client × old/new server 四象限中，old-client→current-server、current-client→current-server（`compat-runtime-fixtures`）与 current-client→old-server（`compat-old-server-runtime-fixtures`）三个非平凡方向均有真实 `mem_service_handle_operation` + 真实 metrics/fail-closed 计数的运行时证据；old-client→old-server 为前者的严格子集，由兼容单调性蕴含。

### 3.2 持久化与可靠性工程

- 当前持久化仍偏最小实现（snapshot + journal + storage_root），未达到生产级 durable catalog 要求。
- **已闭环（2026-06-27，4.2a）**：catalog schema 版本化 + 前向兼容迁移策略（`catalog_schema_version=1`，serve 启动期 `mem_service_check_catalog_schema_version` 接受当前版本、拒绝未知未来版本，`migration_policy=catalog-schema-version-accept-current-reject-future`）；journal 原子写入屏障（`append_journal` 增加 `fflush`+`fsync`）+ 撕裂尾部恢复（`load_journal` 在 EOF 处丢弃未闭合的 torn trailing frame 而非 brick 重启，新增 `journal-torn-recovery-fixtures` 用真实文件 I/O 证明）。
- **已闭环（2026-06-27，4.2b / 2026-06-29 增量）**：chunked sealed block backend（`sealed-chunked-block-v1`，payload_kind=65）：大 payload 按 1024B 分块写入 `blocks/<checksum>.chunked/` + manifest（chunk_count/chunk_size/total_len/total_checksum），validate 按序重组并重算 FNV-1a 64-bit 校验、任何不一致即 fail-closed quarantine 整目录。新增 `chunked-block-fixtures` 用真实文件 I/O 证明写/校验/破坏后隔离。`payload_block_backend` 契约扩展为 `sealed-local-block-v1,sealed-chunked-block-v1,transport-loopback-block-v1,transport-tcp-block-v1`；其中 `transport-loopback-block-v1`（payload_kind=66）提供 transport backend 骨架，写入 `remote-blocks/<checksum>.transport/` + manifest，`transport-block-fixtures` 证明写/校验/破坏后 fail-closed quarantine；`transport-tcp-block-v1`（payload_kind=67）从 `tcp:<ipv4>:<port>` 拉取 payload，`network-transport-block-fixtures` 用真实 TCP loopback source 证明 fetch/write/validate/破坏后 quarantine。
- **已闭环（2026-06-27，4.2c）**：journal 有界 compaction 策略（`journal_truncation_policy=threshold-compaction`）：成功 save_store 后，若 journal 超过 `MEM_SERVICE_JOURNAL_COMPACTION_THRESHOLD_BYTES`（4096）即原子重写为 header-only（snapshot 已是 source of truth，不破坏 cross-restart replay）。新增 `journal-compaction-fixtures` 用 65 次真实 PUT 证明 journal 被界定在阈值内、旧事件被 compact 掉、且 snapshot 恢复 + idempotency replay 仍成立。
- **已闭环（2026-06-29，4.2d）**：product restore admission 补齐为发布级门禁。新增 `restore-policy-fixtures`，用真实 `mem_service_handle_operation` 证明 full snapshot 与 paged snapshot 都走 transactional staged restore；bad magic 返回 `invalid_session`、out-of-order page 返回 `version_conflict`、record-count mismatch 返回 `version_conflict`、cancelled stage commit 返回 `invalid_session`，四类负例均计入 fail-closed，且 live `restore-policy-anchor` 在有效 commit 前保持不变。`restore_policy=transactional-staged-restore`、`restore_policy_gate=restore-policy-fixtures` 已进入 release/package manifest、host-artifact-smoke 与 install-smoke。
- remote（远端 transport）block backend 已从纯 policy 文本推进到 loopback + TCP loopback 数据面：`remote_payload_block_backend=transport-loopback-block-v1,transport-tcp-block-v1`、`remote_payload_block_data_gate=transport-block-fixtures`、`remote_payload_network_transport=tcp-loopback-certified`、`remote_payload_network_transport_gate=network-transport-block-fixtures`、`remote_payload_network_transport_make_gate=network-transport-block-smoke` 与 `remote_payload_block_backend_gate=remote-block-backend-policy-fixtures` 进入 release manifest。跨主机生产网络 transport 仍需真实部署验证，当前新增 `remote_payload_production_network_transport=not-certified`、`remote_payload_production_transport_evidence_schema=remote-transport-evidence-v1`、`remote_payload_production_transport_evidence_gate=remote-transport-evidence-fixtures`、`remote_payload_production_transport_generate=remote-transport-generate-evidence`、`remote_payload_production_transport_verify=remote-transport-verify --evidence-file`、`remote_payload_production_transport_ci=scripts/run_mem_service_remote_transport_ci.sh`、`remote_payload_production_transport_evidence_verify=scripts/verify_mem_service_remote_transport_evidence.sh`、`remote_payload_production_transport_bundle=remote-transport-certification-bundle`、`remote_payload_production_transport_bundle_verify=remote-transport-certification-bundle-verify` 和 `remote_payload_production_transport_bundle_script=scripts/verify_mem_service_remote_transport_bundle.sh`。generator 会真实拉取 producer TCP payload、验证 sealed copy、破坏本地 block 证明 fail-closed，并拒绝 loopback source；CI wrapper 封装 build/generate/verify/bundle/bundle-verify；evidence verifier wrapper 支持 CI artifact 发布后的独立复验；bundle target 只归档已验证 evidence 与发布 manifest，bundle verifier 会安全解包并复验内嵌 evidence；verifier 用可复验 evidence 边界避免把 loopback TCP 误读为多机网络已认证。

### 3.3 wire/接口演进深度

- payload schema 仍以文本 key/value 为主，未完成更完整的二进制/强 typed 数据面演进验证。**已闭环（2026-06-27，4.5）**：新增 typed-binary 编解码（TLV + 版本门禁）与 text-kv 并存，`typed-payload-fixtures` 真实字节级 round-trip + 前向兼容版本拒绝 + 畸形输入 fail-closed，对抗 review 零 bug。
- **已闭环（2026-06-29，4.3 增量）**：serving 与 pretraining 的 session/model/artifact-kind/artifact-id/owner/version/checksum 负例已形成独立完整回归矩阵——`serving-fail-closed-fixtures`（runtime-handoff + execution-artifact 各跑全 mismatch 矩阵）与 `pretraining-fail-closed-fixtures`（training-step-commit 全 mismatch 矩阵），用真实 `mem_service_handle_operation` + 真实 fail-closed 计数承载（serving fail_closed=12、pretraining fail_closed=6）。owner 维度通过 artifact query 的 `expected_owner` 显式表达，owner mismatch 返回 `invalid_model_binding` 并计入 fail-closed。release-manifest 记录 `payload_ownership_matrix=certified`、`payload_ownership_scope=artifact-query-expected-owner` 与 `payload_ownership_gate=serving-fail-closed-fixtures,pretraining-fail-closed-fixtures`，并进入 host-artifact-smoke/install-smoke 门禁。

### 3.4 运维与部署闭环

- 真实系统级部署门禁尚未补齐（真实 systemd 环境、真实生产采集/告警联调还未通过真实环境认证）。**已补齐本地可做的硬边界（2026-06-29，4.4a/4.4b/4.4c/4.4d/4.4f/4.4i/4.4j/4.4k/4.4l/4.4m/4.4n/4.4p/4.4q）**：新增 `ops-certification-policy`、`ops-certification-fixtures`、`ops-certification-evidence-fixtures`、`ops-certification-generate-evidence`、`ops-certification-linux-ci-smoke`、`linux-ops-certification-smoke`、`linux-ops-upgrade-rollback-smoke`、`linux-ops-deployment-smoke`、`linux-ops-certification-bundle`、`linux-ops-certification-bundle-verify`、`release-certification-verify`、`scripts/run_mem_service_linux_ops_ci.sh`、`scripts/verify_mem_service_linux_ops_evidence.sh`、`scripts/verify_mem_service_ops_certification_bundle.sh`、`scripts/run_mem_service_remote_transport_ci.sh`、`scripts/verify_mem_service_remote_transport_evidence.sh`、`scripts/verify_mem_service_remote_transport_bundle.sh`、`scripts/verify_mem_service_release_certification.sh` 和 `ops-certification-verify --evidence-file <path>`，将 `linux-systemd-service-smoke`、`linux-systemd-host-service-smoke`、`prometheus-scrape-smoke`、`prometheus-alertmanager-rule-smoke`、`rpm-package-smoke`、`upgrade-rollback-deployment-smoke` 固化为 release/package/install 门禁，并提供外部 Linux CI 证据生成、落盘、独立复验、认证证据包归档、证据包复验与整体验收入口；这些复验脚本已随 installed package 发布到 `share/lingqu/mem_service/scripts/`，发布后的 tar/deb/rpm 不再需要源码树才能定位复验入口。当前 release manifest 明确 `real_systemd_environment=not-certified`、`production_collector_alert_environment=not-certified`、`rpm_package=not-certified`。**环境阻塞**：本机为 macOS dev，无 systemd/rpm/rpm2cpio，4.4 的"真实"通过仍需真实 Linux CI 运行 `scripts/run_mem_service_linux_ops_ci.sh --rollback-rpm <previous-rpm>` 并产出可通过 `scripts/verify_mem_service_linux_ops_evidence.sh --evidence-file <evidence>` 复验的 evidence 文件；现状已有 systemd-like + collector + alert 的 simulator 级门禁（`installed-host-service-manager-smoke`、`collector-fixtures`、`alert-integration-fixtures`）。
- 包分发的 rpm 入口已补齐为可复用发布门禁：`packaging/linqu-mem-service.spec`、`package-rpm`、`package-rpm-smoke` 已进入 release/package contract；**环境阻塞**：rpm 真实产物验收仍需 `rpmbuild`/`rpm2cpio`/`cpio` 的 Linux rpm toolchain，本机 macOS 只验证缺工具时 fail-closed；deb + tar 双渠道已就绪。
- systemd 默认配置安装布局已补齐：`install` 会把 `configs/mem_service.runtime.conf` 作为 runtime config 安装到 `etc/lingqu/mem_service/mem_service.conf`，同时把 runtime config 原件放入 `share/lingqu/mem_service/config/` 并保留 `/tmp` 路径的 example config 作为开发样例；host runtime config 则由 `configs/mem_service.host.runtime.conf` 安装到 `etc/lingqu/mem_service/mem_service.host.conf`。release/package manifest 记录 `runtime_config`、`runtime_config_source`、`host_runtime_config` 与 `host_runtime_config_source`，tar/deb/rpm package smoke 均验证这些文件存在。该项解决了 unit 文件指向 `/etc/...` 但安装包只带 share/example config 的部署缺口。
- 无认证模式的本地安全边界已补齐：`auth_mode=none` 只允许 `listen=unix:<path>` 的服务入口，`metrics_listen` 只允许 `tcp:127.0.0.1:<port>`；config parser、daemon metrics listener、`config-fixtures`、release/package manifest 和 install-smoke 均固化 `service_auth_boundary=unix-socket-local-only`、`metrics_auth_boundary=loopback-only` 与 `config_security_gate=config-fixtures`。这不是 TLS/ACL 认证完成，而是防止独立部署时把无认证服务或 metrics 误暴露到非 loopback 网络。
- 二进制自描述门禁已补齐：`version` 与 `version-fixtures` 使安装后的 `linqu_mem_service` 和 `linqu_mem_service_host` 能直接报告当前 release/API/wire/package contract 指纹；`package-manifest` 将 `binary_version_command=version`、`binary_version_contract=text-kv`、`binary_version_gate=version-fixtures` 和 `required_gate=version-fixtures` 固化为发布要求。
- 安装后 SDK runtime gate 已补齐：`installed-sdk-runtime-smoke` 使用安装后的 host daemon 与安装后编译出的 serving/pretraining 示例客户端进行真实运行时交互，覆盖 serving prefix/KV/runtime handoff/execution artifact 和 pretraining artifact/step commit 路径；release/package manifest 将该 gate 固化为 required gate。它不是 systemd 生产认证，但证明独立发布布局下的 daemon 与外部客户端可以协同运行。
- 标准 systemd unit 安装布局已补齐：`install` 会把 `linqu_mem_service.service` 与 `linqu_mem_service.host.service` 安装到 `usr/lib/systemd/system/`，同时保留 `share/lingqu/mem_service/deploy/` 下的 contract manifest 副本；两个 unit 声明 `RuntimeDirectory=lingqu`，普通 service 使用 `StateDirectory=lingqu/mem_service`，host service 使用 `StateDirectory=lingqu/mem_service_host`。release/package manifest 记录 `systemd_unit` 与 `host_systemd_unit`，tar/deb/rpm package smoke 均验证 unit 随包落盘。真实 `systemctl enable/start/status` 仍属于 Linux CI 外部认证范围。

## 4. 补齐方案（执行顺序）

### 4.1 兼容性矩阵补齐（优先）—— ✅ 已完成（2026-06-27）

1. ✅ 增加 old/new client 与 old/new server 的 full matrix 运行时门禁：新增 `compat-old-server-runtime-fixtures`，覆盖 current-client→old-server 方向；与既有 `compat-runtime-fixtures`（old/current-client→current-server）共同构成非平凡组合矩阵。
2. ✅ 引入旧 server runtime 变体：在 `struct mem_service` 上新增 `enforce_expected_context`（默认 true，保持生产行为），old-server 变体仅在该实例上置 false；`mem_service_query_artifact` 的六项 `expected_*` 校验被该开关包裹，old server 因此容忍 extended-profile 查询。证据以真实 `mem_service_handle_operation` 调用 + `fail_closed_count`/`stale_ref_count`/`checksum_mismatch_count`/`invalid_model_binding_count` 计数对比承载（current_fail_closed=4 vs old_fail_closed=0）。
3. ✅ 纳入 release/in-house smoke：接入 `host-artifact-smoke`、`install-smoke`（新增 `compat_old_server_runtime_gate` grep 与 evidence grep），并联动 release/package/upgrade-rollback/api-abi/compat-baseline/compat-old-new 契约的 checksum/len 重算与 Python 门禁。

### 4.2 后端持久化工程增强

1. ✅ 在现有 storage_root 上补齐 durable catalog migration（2026-06-27，4.2a）：`catalog_schema_version` 写入 `catalog/manifest.txt`，serve 启动期 `mem_service_check_catalog_schema_version` 接受当前/缺失版本、拒绝未知未来版本；`durable-catalog-fixtures` 用真实文件 I/O 同时证明接受 v1 与拒绝 v99。`upgrade-rollback-policy` 契约 `migration_policy` 由 `not-yet` 翻转为真实策略。
2. ✅ journal truncation 与原子写入策略（2026-06-27，4.2a+4.2c）：
   - ✅ 原子写入（4.2a）：`append_journal` 增加 `fflush`+`fsync` 屏障；`load_journal` 在 EOF 丢弃未闭合的 torn trailing frame（崩溃恢复，不再 brick 重启）；新增 `journal-torn-recovery-fixtures` 用真实 torn 文件证明。
   - ✅ 有界 compaction（4.2c）：`compact_journal` 在 save_store 成功后、journal 超 `MEM_SERVICE_JOURNAL_COMPACTION_THRESHOLD_BYTES` 时原子重写为 header；新增 `journal-compaction-fixtures` 证明界定 + 恢复。`journal_truncation_policy=threshold-compaction` 写入 compat-matrix/compat-baseline 契约。
3. ✅ 增加分片 block backend 的最小可行实现并接入兼容门禁（2026-06-27，4.2b）：`sealed-chunked-block-v1`（payload_kind=65），1024B 分块 + manifest，validate 重组重算 FNV-1a 64-bit + fail-closed quarantine；`chunked-block-fixtures` 真实 I/O 证明（该 fixture 在开发期捕获并修复了 2 个真实 bug：末块非整块校验、quarantine 路径）。2026-06-29 增加 `transport-loopback-block-v1`（payload_kind=66）和 `transport-tcp-block-v1`（payload_kind=67），分别由 `transport-block-fixtures` 与 `network-transport-block-fixtures` 证明；同日新增 `remote-transport-evidence-fixtures`、`remote-transport-generate-evidence`、`remote-transport-verify --evidence-file`、`scripts/run_mem_service_remote_transport_ci.sh`、`scripts/verify_mem_service_remote_transport_evidence.sh`、`remote-transport-certification-bundle`、`remote-transport-certification-bundle-verify` 与 `scripts/verify_mem_service_remote_transport_bundle.sh`，把跨主机生产 network transport 保持为外部 evidence 门禁，并提供 CI artifact 归档/发布后复验入口。
4. ✅ restore policy 发布门禁（2026-06-29，4.2d）：`restore-policy-fixtures` 覆盖 full snapshot 与 paged snapshot 的 transactional staged restore，证明 bad magic、out-of-order page、record-count mismatch、cancelled stage commit 全部 fail-closed，且 live state 在有效 commit 前不被污染。

### 4.3 serving/pretraining 语义负例闭环 —— ✅ 已完成（2026-06-29）

1. ✅ 独立补充 session/model/artifact/owner mismatch 的 fixture：`serving-fail-closed-fixtures`（runtime-handoff + execution-artifact，全 6 类 mismatch）与 `pretraining-fail-closed-fixtures`（training-step-commit，全 6 类 mismatch），serving/pretraining 各自覆盖。
2. ✅ 将负例 fail-closed 与计数统计固化到 contract：`serving_fail_closed_matrix=certified` / `pretraining_fail_closed_matrix=certified` + gate 名写入 release-manifest，接入 host-artifact-smoke + install-smoke grep；计数（invalid_session/invalid_model_binding/stale_ref/checksum_mismatch/fail_closed）由真实 metrics 路径承载。
3. ✅ payload ownership gate 已闭环：artifact query 新增可选 `expected_owner`，wire schema 显式记录在 runtime handoff、execution artifact 与 training artifact query 上；owner mismatch 使用 `invalid_model_binding` fail-closed，release/package manifest 记录 `payload_ownership_matrix=certified` 和 `payload_ownership_scope=artifact-query-expected-owner`。

### 4.4 生产运维闭环

1. ✅ 本地可做的发布级 fail-closed 门禁已补齐（2026-06-29，4.4a）：`ops-certification-policy` 明确真实 systemd / Prometheus / Alertmanager / rpm / deployment upgrade-rollback 的外部证据要求，并接入 CLI、host-artifact-smoke、install-smoke、package/release manifest。
2. ✅ 外部证据验收接口已补齐（2026-06-29，4.4b）：`ops-certification-verify --evidence-file <path>` 校验 Linux CI 证据文件，要求 evidence version、service/scope、`evidence_os=linux`、`evidence_init=systemd`、当前 policy/package checksum，以及 6 个外部门禁均为 `pass`；`ops-certification-evidence-fixtures` 覆盖正例和 fail-closed 负例。
3. ✅ 外部证据生成入口已补齐（2026-06-29，4.4c）：`ops-certification-generate-evidence --rpm-file <path> --upgrade-rollback-marker <path>` 在运行环境内探测 Linux/systemd、systemctl 服务状态、`/metrics` scrape、promtool alert rules、rpm2cpio 可解析 rpm，以及 CI 生成的 upgrade/rollback marker；任一条件不满足即输出 fail evidence，后续 verifier 会拒绝认证。
4. ✅ 一键 Linux CI 认证门禁已补齐（2026-06-29，4.4d）：`ops-certification-linux-ci-smoke --rpm-file <path> --upgrade-rollback-marker <path> --evidence-file <path>` 在同一命令内生成 evidence、写入 evidence 文件、再调用同一 verifier 规则验收；任何真实 systemd/rpm/Prometheus/升级回滚条件不满足都会 fail-closed，并保留 evidence 文件用于 CI artifacts。
5. 补齐真实系统部署验收（systemd 生命周期、指标采集、告警联调脚本），需要 Linux CI 实际安装/启动服务并运行 `ops-certification-linux-ci-smoke`。
6. ✅ rpm 发布入口已补齐（2026-06-29，4.4e）：新增 `packaging/linqu-mem-service.spec`、`package-rpm`、`package-rpm-smoke`，release/package manifest 记录 rpm 产物名、架构、payload、gate 与 `requires-linux-rpm-toolchain` 运行边界；本机缺 rpm 工具链时 fail-closed，不伪造认证。真实 rpm 产物一致性仍需 Linux rpm toolchain 执行 `package-rpm-smoke`。
7. ✅ Linux CI 编排入口已补齐（2026-06-29，4.4f）：新增 `linux-ops-certification-smoke`，按固定顺序执行 `package-rpm-smoke`、要求真实升级/回滚 marker 已存在，再调用 `ops-certification-linux-ci-smoke` 生成并验证 evidence；本机缺 rpm/systemd/marker 时 fail-closed，不伪造真实运维认证。
8. ✅ 默认 runtime config 安装闭环已补齐（2026-06-29，4.4g）：`install`、tar/deb/rpm package payload、release/package manifest、install-smoke 和 package smoke 均覆盖 `etc/lingqu/mem_service/mem_service.conf`，与 systemd unit 的 `--config /etc/lingqu/mem_service/mem_service.conf` 默认路径对齐；正式 runtime config 来自 `configs/mem_service.runtime.conf`，默认使用 `/run/lingqu/mem_service.sock` 与 `/var/lib/lingqu/mem_service`，不再复用 `/tmp` 示例配置。host runtime config 来自 `configs/mem_service.host.runtime.conf`，默认使用 `/run/lingqu/mem_service_host.sock`、`/var/lib/lingqu/mem_service_host` 和 metrics port `9901`。
9. ✅ 标准 systemd unit 安装闭环已补齐（2026-06-29，4.4h）：`install`、tar/deb/rpm package payload、release/package manifest、install-smoke 和 package smoke 均覆盖 `lib/systemd/system/linqu_mem_service.service` 与 `lib/systemd/system/linqu_mem_service.host.service`，使包安装后具备直接启用的 systemd unit 文件；unit 声明 `RuntimeDirectory=lingqu`，普通 service 使用 `StateDirectory=lingqu/mem_service`，host service 使用 `StateDirectory=lingqu/mem_service_host`，让 Linux systemd 管理默认 socket/state 目录。
10. ✅ 真实 Linux 部署编排入口已补齐（2026-06-29，4.4i）：新增 `linux-ops-deployment-smoke`，在 Linux/root/systemd/rpm/curl/promtool 环境中安装构建出的 rpm、`systemctl daemon-reload`、同时启动 service/host service、分别 scrape `9900` 与 `9901` metrics、检查 Prometheus 规则，并调用 `ops-certification-linux-ci-smoke` 生成/验证 evidence；本机 macOS 或非特权环境 fail-closed。
11. ✅ 真实 upgrade/rollback marker 生成入口已补齐（2026-06-29，4.4j）：新增 `linux-ops-upgrade-rollback-smoke OPS_CERTIFICATION_ROLLBACK_RPM=<previous-rpm>`，在真实 Linux/root/systemd/rpm/curl 环境中执行 current rpm install -> 双 unit scrape -> rollback rpm install with `--oldpackage` -> 双 unit scrape -> current rpm reinstall -> 双 unit scrape，全部通过后才写入 `upgrade_rollback_deployment_smoke=pass` marker；`linux-ops-deployment-smoke` 依赖该 target，不再接受手工预置 marker 作为真实认证证据。
12. ✅ Linux CI wrapper 已补齐（2026-06-29，4.4k/4.4w）：新增 `scripts/run_mem_service_linux_ops_ci.sh --rollback-rpm <previous-rpm> [--rpm-file <current-rpm>] [--out-dir <dir>]`，作为真实 Linux CI 推荐入口。源码树模式封装 `PACKAGE_OUT_DIR` 与 `OPS_CERTIFICATION_ROLLBACK_RPM`，按顺序运行 `linux-ops-deployment-smoke` 与 `linux-ops-certification-bundle`；安装后模式从 `share/lingqu/mem_service/scripts/` 自定位同前缀 installed host binary 与 release/package/ops policy manifests，要求 `--rpm-file <current-rpm>` 指向已发布 current rpm artifact，然后直接执行 current rpm install、双 systemd service restart、metrics scrape、rollback rpm install、current rpm reinstall、promtool 规则检查、`ops-certification-linux-ci-smoke`、verified evidence bundle 归档和 installed bundle verifier，不再需要源码树 Makefile。两种模式都会明确输出 `ops-certification-linux-ci.evidence`、`ops-certification-upgrade-rollback.marker` 与 `linqu-mem-service-ops-certification-bundle.tar` artifact 路径；`--dry-run` 可在非 Linux 环境验证命令展开。
13. ✅ Linux CI evidence 独立复验入口已补齐（2026-06-29，4.4l）：新增 `scripts/verify_mem_service_linux_ops_evidence.sh --evidence-file <path> [--app-dir <dir>]` 和 Make target `linux-ops-evidence-verify OPS_CERTIFICATION_EVIDENCE=<path>`，用于 CI 后或跨机器拿到 artifact 后重新构建/定位 `linqu_mem_service_host` 并调用 `ops-certification-verify --evidence-file <path>`。它不接受人工状态覆盖，evidence 缺失或 verifier 拒绝都会 fail-closed，并以 `linux_ops_evidence_verify=linux-ops-evidence-verify` 进入 release manifest。
14. ✅ Linux CI 认证证据包归档入口已补齐（2026-06-29，4.4m）：新增 Make target `linux-ops-certification-bundle`，在 `package-rpm-smoke` 与 `linux-ops-evidence-verify` 都通过后，归档 verified evidence、upgrade/rollback marker、rpm、release manifest、package manifest 和 ops policy 到 `linqu-mem-service-ops-certification-bundle.tar`，并写入 `ops-certification-bundle.manifest`。该 target 只打包已通过验证的证据，不生成或覆盖认证状态；evidence 无效、marker 缺失或 rpm 不可验收都会 fail-closed。
15. ✅ Linux CI 认证证据包独立复验入口已补齐（2026-06-29，4.4n）：新增 `scripts/verify_mem_service_ops_certification_bundle.sh --bundle-file <path> [--app-dir <dir>] [--work-dir <dir>]` 和 Make target `linux-ops-certification-bundle-verify OPS_CERTIFICATION_BUNDLE=<path>`，用于发布后或跨机器拿到 bundle 后重新验证 tar 结构、bundle manifest、evidence/marker/policy/manifest 文件存在性，并从解包出的 evidence 再调用 `ops-certification-verify --evidence-file <path>`。它会拒绝绝对路径或 `..` 的 unsafe tar entry，避免把 bundle 解包变成信任边界。
16. ✅ remote transport 认证证据包归档与复验入口已补齐（2026-06-29，4.4o）：新增 Make target `remote-transport-certification-bundle`，在 `remote-transport-evidence-verify` 通过后归档 `remote-transport.evidence`、release manifest、package manifest 和 `remote-transport-bundle.manifest` 到 `linqu-mem-service-remote-transport-bundle.tar`；新增 `scripts/verify_mem_service_remote_transport_bundle.sh --bundle-file <path> [--app-dir <dir>] [--work-dir <dir>]` 和 Make target `remote-transport-certification-bundle-verify REMOTE_TRANSPORT_BUNDLE=<path>`，用于发布后安全解包、检查 bundle manifest，并调用 `remote-transport-verify --evidence-file <path>` 复验内嵌 evidence。它不生成 evidence，也不把 loopback TCP 结果提升为生产跨主机认证。
17. ✅ release 级整体验收入口已补齐（2026-06-29，4.4p；2026-06-30 加固）：新增 `scripts/verify_mem_service_release_certification.sh --ops-bundle-file <path> --remote-transport-bundle-file <path> [--app-dir <dir>] [--work-dir <dir>]` 和 Make target `release-certification-verify`，用于发布前/发布后一次性复验 Linux ops 认证 bundle 与 remote transport 认证 bundle。它先安全解包并调用两个 bundle verifier，再把解出的 `ops-certification-linux-ci.evidence` 与 `remote-transport.evidence` 交给 `release-readiness --ops-evidence-file --remote-transport-evidence-file` 做最终放行判定；不生成 evidence，不接受人工状态覆盖，任一 bundle 缺失、复验失败或 readiness 未达到 `overall_status=certified` 都会 fail-closed。
18. ✅ 发布包复验脚本安装闭环已补齐（2026-06-29，4.4q/4.4r/4.4s/4.4t/4.4u/4.4v/4.4w/4.4x/4.4y）：`install` 会把 Linux ops、remote transport、bundle verifier、release certification verifier、release certification CI wrapper、installed-layout selfcheck 与 installed SDK verifier 共 10 个脚本安装到 `share/lingqu/mem_service/scripts/`；`package-manifest` 记录 `release_script_root`、10 个 `release_script`、`linux_ops_ci`、`linux_ops_ci_preflight`、`remote_payload_production_transport_ci`、`remote_payload_production_transport_ci_preflight`、`release_certification_ci`、`release_certification_preflight`、`release_certification_readiness_gate` 和 `file_class=release_scripts count=10`，`release-manifest` 记录同一脚本清单、独立 Linux ops CI/preflight 入口、独立 remote transport CI/preflight 入口、installed SDK verifier、发布级 CI/preflight 入口和 readiness 最终放行入口，tar/deb/rpm package smoke 与 install-smoke 均验证脚本随包落盘且可执行。`verify_mem_service_installed_layout.sh --no-runtime` 可在不依赖源码 checkout 的情况下，从安装后的 `share/` 目录反向定位同前缀 `bin/`、`libexec/`、`etc/`、`systemd`、manifest、config、deploy 与 release scripts 并执行结构自检。`verify_mem_service_installed_sdk.sh` 可从安装后的 `share/lingqu/mem_service/scripts/` 自定位同前缀 `pkg-config` 元数据、headers、SDK sources、examples 与 host daemon，用 installed `.pc` 编译 serving/pretraining 外部客户端，并默认跑通安装后 daemon 的 runtime 协同；它让发布包具备不依赖源码 checkout 的 SDK 消费复验入口；其 `--preflight` 会在不编译、不启动 daemon 的情况下检查 compiler、`pkg-config --define-prefix --exists`、非空 `Cflags`、非空 `sdk_sources`、serving/pretraining examples 与 installed host binary。`run_mem_service_remote_transport_ci.sh` 在安装后路径下会自定位 installed host binary 与 installed release/package manifests，生成 remote transport evidence 后直接归档并调用 installed bundle verifier 复验，不再需要源码树 Makefile。`run_mem_service_linux_ops_ci.sh` 在安装后路径下也会自定位 installed host binary 与 installed manifests，要求 `--rpm-file <current-rpm>` 指向当前发布 rpm，并直接执行真实 Linux ops 部署、升级回滚、evidence 生成、bundle 归档和 installed bundle verifier，不再需要源码树 Makefile。`run_mem_service_linux_ops_ci.sh --preflight` 会在独立执行 Linux ops 认证前检查 Linux/root/systemd、rpm/promtool 工具链、rollback rpm、安装后 current rpm、installed host/manifests，失败即 fail-closed；`run_mem_service_remote_transport_ci.sh --preflight` 会在独立执行跨 host transport 认证前检查 partition marker、non-loopback source、producer/consumer 分离，以及源码树模式下的 `make` 或安装后模式下的 installed host binary/manifests，失败即 fail-closed；`run_mem_service_release_certification_ci.sh` 将 installed SDK gate、Linux ops CI、remote transport CI、release certification verify 与 release-readiness 最终 gate 串成一个发布级 CI 入口，避免发布流程只跑单边认证，并可通过 `--rpm-file <current-rpm>` 将发布 rpm 传给 installed Linux ops CI；其 `--preflight` 会在执行 rpm/systemd/remote 真实动作前先做顶层 Linux/root/systemd、rpm/promtool 工具链、rollback rpm、network partition marker、non-loopback source 和 producer/consumer 分离检查，顶层通过后继续调用 installed SDK、Linux ops 与 remote transport wrapper 的 `--preflight`，避免发布级门禁与子阶段门禁漂移；`--preflight --dry-run` 可在非 Linux 环境打印这三个子 preflight 命令，验证 installed/source-tree 参数路由。evidence/bundle/release verifier 和 installed release CI wrapper 默认从安装后的 `share/lingqu/mem_service/scripts/` 反向定位同前缀 `libexec/lingqu/mem_service/linqu_mem_service_host`；只有显式 `--app-dir` 或安装 binary 不存在时才使用源码树 app dir。该项把"源码树里有复验脚本"推进为"发布包自带、发布包可自定位的复验入口"。

### 4.5 数据面演进（非替代，增量）—— ✅ 已完成（2026-06-27）

1. ✅ 保持文本 schema 向后兼容：text-kv 仍为默认 wire payload 格式（`wire_payload_text_kv_format=text-kv`）。
2. ✅ 增加二进制/typed payload 演进路径并设置版本兼容门禁：新增 typed-binary 编解码（TLV：`MSTP` magic + version + 字段数 + 每字段 `[type][name_len][name][value_len BE][value]`，STRING 原始字节 / U32=4 BE / U64=8 BE），`typed-payload-fixtures` 用真实字节级 encode→decode 证明 string/u32(>2^31)/u64(>2^63) 全 round-trip、未知未来 version 被 reject、truncated/bad-magic 输入 fail-closed。对抗 review 未发现 bug（边界/溢出/越界均正确 fail-closed）。`wire_payload_typed_binary_format=typed-binary-v1` 固化到 release-manifest。

## 5. 达成度结论

- 当前达成度：`~98%`（2026-06-30 更新：无认证模式的本地安全边界已固化为 config/release/package/install 门禁；restore policy 已从文档缺口补齐为 transactional staged restore 门禁；payload ownership gate 已进入 serving/pretraining fail-closed 矩阵；安装后二进制自描述 contract 已进入 release/package/install 门禁；安装后 SDK runtime smoke 已证明 daemon 与外部 serving/pretraining 客户端可实际协同；安装后 `pkg-config` 元数据已让外部集成具备机器可发现且已被 smoke 实际消费的 SDK include/source 契约；installed SDK preflight 已进入 release/package manifest，发布系统可以在不编译、不启动 daemon 前机器发现并检查该 contract；`release-readiness` 已把当前 release 放行状态、外部证据阻塞项和 evidence 文件复验后的最终 certified 判定变成机器可读 contract；所有进程内可做的协同安全缺口已基本完成）
- 当前状态：**可独立运行、可基本协同**，剩余缺口全部是**环境/外部依赖**阻塞：
  - ~~跨版本组合兼容闭环~~ ✅（4.1）
  - ~~durable 后端工程化（4.2）~~ ✅ 基本闭环（4.2a/b/c/d）；remote block backend 已有显式 fail-closed release contract，真实远端实现仍需 transport 层
  - ~~serving/pretraining 负例矩阵与 payload ownership gate（4.3）~~ ✅（4.3）
  - 真实运维场景硬门禁（4.4）：✅ 本地 fail-closed admission policy、外部 evidence generator/verifier、一键 Linux CI smoke gate、rpm package target、upgrade/rollback marker 生成 target、Linux CI 真实部署编排 target、可复用 CI wrapper、evidence artifact 复验 wrapper、认证证据包归档 target 与认证证据包复验 wrapper 已接入 release/package/install；**真实通过仍环境阻塞**（需真实 Linux CI：systemd + rpm toolchain + rollback rpm 运行 `scripts/run_mem_service_linux_ops_ci.sh --rollback-rpm <previous-rpm>`，并用 `scripts/verify_mem_service_linux_ops_evidence.sh --evidence-file <evidence>` 或 `scripts/verify_mem_service_ops_certification_bundle.sh --bundle-file <bundle>` 复验产物），simulator 级门禁已就绪
  - ~~数据面 typed-binary 演进（4.5）~~ ✅（4.5）
- 数据面演进（4.3 负例矩阵、4.5 typed payload）作为增量项继续推进。

该文件可直接作为后续迭代拆分任务和 release gate 的依据。
