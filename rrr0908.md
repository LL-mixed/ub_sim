# GVA/GSVA 服务能力补齐计划：开工前评估与待澄清问题

日期：2026-09-08（第三轮，对照计划修订 3）
评估对象：`docs/plans/2026-09-08-gva-gsva-service-completion-plan.md`（修订 3）

## 第三轮总体结论

**可以开始 M1 实现，无剩余设计阻塞，也无新增疑问。** 第二轮提出的 R2-1、R2-2
已被修订 3 采纳并解决：§3.5 更正了"已有 TCP control transport"的错误表述，
新增 §3.5.1 完整定义 wire-over-TCP 控制通道与接入策略，§6.2 将其列入 M1.1 范围
并配套 T-control 行为用例组，§9 处置表标记为"设计已定、实现未开始"。

本轮对修订 3 新增/改动内容做了源码复核，全部关键事实成立：

| 复核项 | 结果 |
| --- | --- |
| wire header 48-byte、负载上限 4096（§3.4） | 成立：`MEM_SERVICE_WIRE_HEADER_LEN 48U`、`MEM_SERVICE_WIRE_MAX_PAYLOAD_LEN 4096U` 及 48 字节静态断言 |
| 新 opcode 从 0x70 段分配（§3.4） | 成立：现有 24 个操作占用 1-10/16-19/32-33/48-49/64-65/80-81/96-97，0x70(112) 空闲 |
| `auth_mode=none` 继续限制为本地 Unix（§3.5.1） | 成立：conf.schema 已有 `auth_mode`（enum 仅 none），`listen` 字段注明 auth_mode=none 时必须 unix:；新增 `trusted-guest-network` 是枚举扩展，计划已声明当前解析器不支持 |
| "一次连接完成一次 RPC"沿用现有模型（§3.5.1） | 成立：wire client 现有实现为 connect→单次请求→单次响应→close(fd) |
| 128-byte opaque descriptor、808-byte client record（§3.4） | 成立：`MEM_SERVICE_PROVIDER_DESCRIPTOR_LEN 128U`、`MEM_SERVICE_CLIENT_RECORD_ABI_SIZE 808U` |
| R2-1/R2-2 处置（§3.5、§3.5.1、§6.2、§9） | 已解决：单控制面不变，M1.1 显式新增 wire-over-TCP + 接入策略 + T-control |

开工后仍需按章节执行的确认项（计划已 gate，非澄清问题）：

1. docs 批次提交授权（§7），随后锁定 M1 执行基线完整 SHA 并登记 §8；
2. 远端 readiness 复核：readiness → prepare → readiness（§6.1），恢复后先跑
   固定基线 T-provider 再进 M1 guest 用例；
3. M1.1 编码前核对执行基线的 opcode 0x70 段与 descriptor 编码长度无冲突（§3.4 已要求）；
4. DeepSeek artifact 远端路径/指纹在 M2 前固定（M2 节）；
5. 每笔发布按 §7 逐次申请子仓库/remote/分支/SHA 授权。

---

## 第二轮评估（2026-09-08，对照计划修订 2）

**结论（当时）：可以开始 M1 实现。** 第一轮提出的 A1–A3、B1–B7、C1–C5、D1–D3 共 17 项问题，
计划修订 2 已逐条决策并落到具体章节（见下方对照表与计划 §9），设计层面不再有
未定的架构选择；M1.1 的轻量编码与单元测试不依赖远端 readiness。

剩余事项分两类：

1. **一处新发现的事实性更正（R2-1，建议落入计划后再编码）**：修订 2 §3.5 声称
   "跨 guest 使用已有 TCP control transport"，源码核对不成立，详见下节。
   这不推翻单控制面架构，但需要在 M1.1 范围中显式补上该工作项。
   **（修订 3 已采纳解决，见 §3.5/§3.5.1/§6.2。）**
2. **执行时确认项（不阻塞开工）**：远端 readiness 复核（§6.1 已定流程）、
   docs 批次提交授权（§7）、DeepSeek artifact 远端指纹（M2 前固定，计划已 gate）、
   逐次发布授权（§7）。

修订 2 对第一轮 review 的三项更正（wire 实为 24 个操作、3-node launcher 分支已存在、
kernel allocator 缺区间冲突检查）经第二轮源码复核确认成立，已采纳。

## 第二轮新发现

### R2-1. 跨 guest 控制面 TCP 通道不存在，需显式列入 M1.1 范围

修订 2 §3.5：「guest 间控制连接先复用已有 wire transport，由目标配置指定 endpoint；
本机可使用 Unix socket，跨 guest 使用已有 TCP control transport。」

源码核对结果（mem_service @ e4a8a58）：

- daemon 的 wire 主服务 socket 仅支持 AF_UNIX：`mem_service_daemon.c` 主监听走
  `socket(AF_UNIX, ...)`（约 13051 行），CLI usage 为 `serve --listen unix:<path>`；
- wire client 仅支持 `unix:`：`mem_service_wire_client.c` 只定义
  `MEM_SERVICE_UNIX_SPEC_PREFIX`，connect 走 `sockaddr_un`，无 AF_INET 路径；
- 代码中存在的 TCP 只有两类，均不是控制面 transport：
  payload 数据 transport（`mem_service_provider_tcp.*`，含
  `remote-transport-serve-fixture` 测试夹具）和 metrics listener
  （`mem_service_parse_tcp_listen_spec`，且显式拒绝非 loopback 绑定）。

影响：M1 选定的单控制面拓扑（nodeA daemon + nodeB 客户端 + 各节点 provider 向
控制面注册/上报）下，跨 guest 的 client→daemon 与 provider→daemon 控制消息
目前没有可用通道；AF_UNIX 不能跨 guest。若按现状进入编码，该缺口会在 M1.2
双 guest 联调时才暴露。

需要补充澄清/决定：

- 确认在 M1.1 显式新增 wire-over-TCP 控制通道：client 端 `tcp:` endpoint 解析与
  AF_INET connect、serve 主监听支持配置指定的非 loopback 绑定（现有
  `parse_tcp_listen_spec` 拒绝非 loopback，不能直接复用）、版本兼容与接入策略
  （绑定地址来自配置、仅接受配置内节点）及对应行为测试；
- 或改选其他跨 guest 通道方案并说明理由（如每 guest 本地 unix 入口 + 转发，
  但这会改变 §3.5 已定的单控制面职责划分，不建议默认采纳）。

### R2-2. 次要：控制通道接入策略需在 M1.1 一并定义

metrics listener 的 loopback-only 限制表明 daemon 有意限制非本机绑定。新增控制面
TCP 监听时需要最小接入策略（绑定配置指定 IP、可选的对端节点校验），建议随 R2-1
的决定一并写入 §3.5，避免编码时临时取值。

## 第一轮问题处置对照（修订 2 已决策）

| 编号 | 第一轮问题 | 修订 2 落点 | 本轮状态 |
| --- | --- | --- | --- |
| A1 | n4-910c1 readiness 未恢复 | §6.1 定义 readiness/prepare/readiness 流程与职责 | 已定，执行时复核 |
| A2 | 基线重锚 | §1、§8：ec01ba1 为设计依据，执行锁干净 commit | 已定 |
| A3 | 未提交文件处置 | §6.1、§7：docs 独立批次待授权，其他任务不动 | 已定，提交待授权 |
| B1 | wire 扩展方式 | §3.4：新 opcode 从 0x70 段分配，按操作版本化 | 已定；并更正为 24 个现有操作 |
| B2 | daemon 拓扑与 payload 驻留 | §3.5：单控制面 + 每 guest 常驻 provider，home provider 持有 export | 已定（但见 R2-1） |
| B3 | 固定映射地址来源 | §3.6：服务分配响应携带描述符，kernel 单一地址权威 | 已定 |
| B4 | gva_manager 启动顺序 | §3.6：guest 基础设施段 5 步依赖编排，有界查询等待 | 已定 |
| B5 | 端到端用例落点 | §3.7：CLI 新增 `object-session`，dual-node runner 增加组合用例 | 已定，接口待实现 |
| B6 | UB-SSD 验收口径 | §3.7、M1.3：OBMM/SSD 均要求两客户端跨 guest | 已定 |
| B7 | 资源统计口径 | §3.7：M1 新增 allocation-stats，交叉核对 | 已定 |
| C1 | 真实 epoch/token 通道 | §3.6：provider 读 kernel alloc/query，缺字段显式声明 profile | 已定 |
| C2 | W5 3-node 入口 | M2：复用参数化 launcher `SIM_W5_CLUSTER_NODE_COUNT=3` | 已澄清（第一轮判断有误） |
| C3 | DeepSeek artifact | M2：候选文件名与历史 run 已登记，远端指纹待实测 | 外部事实，M2 前固定 |
| C4 | M5 对齐基线 | M5：以 2026-09-05 tokenless 方案为准，kernel/QEMU 配对已记录 | 已定 |
| C5 | M3 reconciliation 权威源 | M3 缺口表，M1 编码时确认 | 已澄清 |
| D1 | 提交授权范围 | §7：逐次明确子仓库/remote/分支/SHA 后申请 | 已定 |
| D2 | sim-console 集成 | §6.2：M1 不需要 Web UI，CLI/headless 先交付 | 已定 |
| D3 | 提交节奏与最小验证集 | §6.2：M1.1–M1.4 四批次与 T-* 命令集 | 已定 |

## 执行时确认清单（不阻塞开工，按章节执行即可）

1. 远端 readiness：按 §6.1 顺序执行 readiness → prepare → readiness，区分对象缺失 /
   checkout 落后 / 构建缺失 / 探测超时；恢复后先跑一次固定基线 provider 2-node 验收
   再执行 M1 guest 用例。
2. docs 批次：计划、三份设计文档、索引与图像构成独立候选批次，等授权后提交，
   随后锁定 M1 执行基线完整 SHA 并登记到 §8 跟踪表。
3. DeepSeek V4 Flash artifact：M2 前在 n4-910c1 固定路径、字节数、SHA-256、
   tokenizer 与 backend profile；缺文件只报告阻塞，不擅自换量化版本。
4. 发布授权：mem_service/kernel/QEMU 每笔发布按 §7 逐次申请明确范围。

---

## 附：第一轮 review 原文（2026-09-08，计划修订 2 §9 引用的编号来源，保留原样）

日期：2026-09-08
评估对象：`docs/plans/2026-09-08-gva-gsva-service-completion-plan.md`（修订前版本）
评估方式：对照计划逐条核对 ub_sim（HEAD `ec01ba1`）与 mem_service（`e4a8a58`，lock 一致）
源码、guest 脚本与目标机状态。

### 总体结论

**暂不建议直接开始 M1 编码。** 计划的阶段划分、架构约束和验证管理完整，机制基线明确，
但存在三类开工前必须澄清的问题：

- A 类：环境与基线前置（目标机 readiness、基线漂移、未提交文件），阻塞远端验证；
- B 类：M1 的关键设计决策空缺（wire 扩展方式、daemon 部署拓扑、payload 驻留与映射地址来源、
  启动顺序编排、用例落点、验收口径），这些决策未定就直接写代码会产生返工；
- C/D 类：M2+ 需提前对齐的点与提交授权范围（不阻塞 M1 第一天，但影响 M1 的字段预留与节奏）。

计划中无需澄清、可直接依托的既有基础：provider 中立契约与 OBMM provider 的
register/export/map/publish/invalidate/wait-visible/unmap 已实现并有 guest 级 conformance；
gva_manager 的 bootstrap/segment alloc/query/retire 有两节点验收入口；
2-node 拓扑、`run_ub_dual_node_apps.sh` 框架、daemon `serve` 模式的 guest 启动分支均已存在；
`mem_service.lock` 与 gitlink 一致（`e4a8a58`）。

### A 类：环境与基线前置（阻塞远端验证）

#### A1. n4-910c1 readiness 未恢复

2026-09-08 探测结论：目标机 5 个子模块（`mem_service`、`guest-linux/kernel_ub`、
`vendor/qemu_8.2.0_ub`、`vendor/simpler`、`vendor/pto-isa`）版本落后于本地 HEAD 锁定的
gitlink，前端呈 blocked（另有瞬时探测超时滞留的已知问题）。计划 §6 指定 n4-910c1 为主验证目标。

待澄清：

- 是否按既有路径先恢复环境：各子模块 push 到镜像仓库 → Prepare target farm → 前端手动刷新
  重新探测？由谁执行、是否有镜像 push 权限？
- mem_service `e4a8a58` 已在 GitHub `origin/master`，但目标机实际 fetch 的镜像是否同步？
  其余 4 个子模块当前 gitlink（kernel `85fb35d`、QEMU `f201ebe`、libobmm `53011ee`、
  simpler `df0cf4f`、pto-isa `66213f9`）是否均已 push 到各自镜像并可被目标机 fetch？
- M1 首批远端验证是否等 readiness 恢复后再开始，还是先在本地做纯编码、远端验证排队？

#### A2. 基线重锚

计划编写时机制审计基线为 ub_sim `39576eb`（编写时根仓库 `80cb36f`），当前 HEAD 已是
`ec01ba1`（sim-console 双视图改造）。计划要求"执行时必须记录实际完整 SHA 和配置"。

待澄清：M1 开工是否以 `ec01ba1` 重锚基线？跟踪表（计划 §8）是否同步更新基线列？

#### A3. 未提交文件处置

当前工作区有 8 个已修改文件（`crates/sim-console` 样式/测试、`docs/README.md`、
`docs/sim_gva_gsva_implementation_spec.md`、`docs/sim_gsva_shared_virtual_address_design.md`、
`docs/sim_gva_simulation_design.md`、`docs/plans/2026-05-15-...`、`docs/plans/2026-08-29-...`）
和 7 个未跟踪文件（含本计划文档、async-load 2026-09-03 三件套、架构图三件套）。

待澄清：

- 是否先把 docs 批次（本计划 + spec 审计刷新 + 架构图）提交，作为 M1 的文档基线？
  spec 等三份设计文档的未提交修改是否就是本次审计刷新，需一并提交？
- sim-console 两个未提交修改（`visual_smoke.mjs`、`styles.css`）是完成态还是进行中？
  是否需要先收尾或 stash，避免与 M1 的提交混在一起？

### B 类：M1 设计决策空缺（阻塞编码）

#### B1. wire 协议扩展方式

计划 §3.3 列出 allocate/inspect、map/unmap、publish/wait-visible、acquire/release、
retire/revoke、read/write 操作语义，但现有 wire 协议只有 put/get/inspect/materialize-object
等操作（见 `mem_service_wire.h`），客户端 SDK（`mem_service_client.h`）没有任何
allocate/map/acquire/release/retire 入口；计划同时要求"不能直接扩大已有结构或改变
opcode 负载"。

待澄清（二选一或组合，工作量和兼容性差异很大）：

- 允许新增 opcode 与升级 `MEM_SERVICE_WIRE_SCHEMA_VERSION`（新 op 编号段、新 payload schema、
  旧端兼容检查按 §3.3 执行）？
- 还是 allocate 复用 put-object 扩展字段、映射能力完全走 opaque provider descriptor 交换，
  不动 wire op 集合？

#### B2. daemon 部署拓扑与 payload 驻留位置

M1 要求"服务独立启动、payload 在客户端/provider endpoint 之间直接访问、daemon 不中转
payload"。但现状是：daemon 的 put-object 接收 `payload_inline`/`payload_path`，
materialize-object 由 daemon 写出文件，即 daemon 当前是 payload 中转模型；OBMM payload
arena（`mem_service_payload_arena_alloc`）挂在 W5 的 cluster runtime 上，不在 daemon 路径。

待澄清：

- M1 拓扑：每 guest 一个 daemon（客户端连本节点 unix socket，daemon 管理本节点 OBMM pool），
  还是单点控制面 daemon + 各 guest 数据面 provider/agent？"服务可在零 provider 时运行"
  对应哪种？
- 对象 payload 的物理位置与 export 责任：哪个进程拥有 OBMM pool 并调用 export？
  客户端 map 的是对端已 export 的 region，还是自己 pool 中被对端 import 的 region？
  跨 guest 的 descriptor 交换通道是 daemon 转发还是 provider 控制面（conformance 现有
  `exchange_remote_regions` 走 bootstrap 共享目录）？

#### B3. 固定映射地址来源

M1 验收"固定映射"且"严格同址申请失败必须失败"（架构约束 6）。

待澄清：固定地址是客户端经 gva_manager query 取得 GSVA 后自行指定
（`MEM_SERVICE_MAPPING_FLAG_FIXED_ADDRESS` + `requested_address`），还是由服务在 allocate
响应中分配并返回？若是后者，服务作为地址权威与"复用现有 gva_manager 的地址权威"
（架构约束 5）如何分工——服务向 gva_manager 申请 segment，还是只消费已 alloc 的 segment？

#### B4. gva_manager 启动顺序编排

计划要求"核对 gva_manager bootstrap 与独立服务启动顺序，禁止在客户端补初始化"。
现状：gva_manager 由 kernel cmdline（`linqu_gva_manager=1`、`gva_manager_mode=...`）在
run_app 阶段执行；mem_service 的 guest 启动同样是 run_app 分支，两者无依赖关系。

待澄清：服务启动如何等待/校验 gva_manager 已完成 bootstrap 与 segment alloc
（轮询 `/proc/obmm/gsva_aperture` 或 query 接口？新增 handshake？）？
该编排落在 run_app、launcher 脚本还是 daemon 自身的启动检查？

#### B5. M1 端到端用例落点

现状：guest 的 `linqu_mem_service=1` 模式只跑 fixture 自测；OBMM conformance 是 provider
级两节点测试（无 daemon、无 SDK）；serving example 只走元数据寄存/查询，无真实 payload
映射。M1 需要"两 guest、两进程、同一 SDK：分配→固定映射→写→远端可见+checksum→unmap→释放"。

待澄清：

- 新增 guest app（如 `mem_service_object_share`），还是扩展 `linqu_mem_service` CLI？
- 若扩展 CLI：计划 §3.3 要求"映射客户端需保持进程存活直到 unmap"，一次性 CLI 进程模型
  不满足，是否接受增加长驻/交互子命令模式？
- 该用例接入 `run_ub_dual_node_apps.sh` 的 `--app` 清单，还是按 §6 优先扩展已有 CLI
  入口并附测试？

#### B6. UB-SSD 同阶段验收口径

M1 要求"同阶段另测 UB-SSD 分配、显式写读和释放；两者通过同一对象服务访问"。
现有 `put-object --backend ub-ssd-gsva-v1` 已支持 UB-SSD 后端写读。

待澄清：M1 的 UB-SSD 用例复用现有 backend 流程即可（单客户端写读释放），还是同样要求
"两独立客户端跨 guest 显式 I/O"？后者目前没有对应的跨进程 read/write 入口，工作量不同。

#### B7. 资源统计口径

验收要求"释放后资源统计恢复初始值""backing 耗尽有确定结果"。现状：daemon metrics 只有
操作计数，无 arena 字节水位/mapping 计数；payload arena 线性推进无回收（计划 §1 已指出
record 回收不能证明 payload 空间回收）。

待澄清：M1 是否就要补 arena 字节水位与 mapping 计数的对外暴露（status/metrics 扩展，
同样涉及 B1 的 wire 决策），还是 M1 仅以日志与 guest 内核对计数为准、对外统计留到 M3/M4？

### C 类：M2+ 需提前对齐的点（影响 M1 字段预留，不阻塞第一天）

#### C1. 真实 epoch/token 的读取通道

`mem_service_make_gsva_buffer_desc_from_source()` 当前硬编码 `key_epoch=1`、
`token_value=token_id`、`MEM_SERVICE_GSVA_MAX_NODES=8`、VMID/ASID 零值。M2 要接管理端
真实身份。

待澄清：权威来源与读取方——gva_manager query 接口、kernel GSVA UAPI（`gsva.h`）、还是
QEMU `SIM_DEC_OP_GSVA_QUERY_V1`？由 daemon 读、provider 读还是客户端读？
这决定 M1 descriptor/record 需要预留哪些字段，建议 M1 设计时就定。

#### C2. W5 3-node 入口

M2 验收要求 2/3/8-node。现有 W5 guest launcher 只有 2/4/8-node 形态（vendor 有
`ub_topology_three_node_full_mesh.ini` 与 `mvp_3host_single_domain.yaml`）。

待澄清：3-node W5 runner 是新建脚本，还是参数化现有 2/8-node 脚本？3-node 的 PP 分层
配置是否已有定义？

#### C3. 2-bit GGUF DeepSeek V4 Flash artifact

M2 验收以该模型完成 2/3/8-node 4-step、核心配置 8-step，并要求记录模型路径/指纹。

待澄清：该模型 artifact 在 n4-910c1 的路径、指纹与加载方式是否已固定并写入配置？
"已跑通"对应哪次运行证据（用于锁定 baseline 配置）？

#### C4. M5 对齐基线

async-load 的 wait_key 方案（`2026-09-03-async-load-wait-key-lifecycle-design.md`）已被
2026-09-05 tokenless void-response replay contract 取代，该文档自身标注为历史材料。

待澄清：M5"联调前先确定两边共同基线"——参照的 async-load 现行文档是哪份
（2026-09-02/2026-09-05 系列）与哪个代码提交组合？与 async-load 线对齐的时间点和接口人？

#### C5. M3 reconciliation 权威源

daemon journal 覆盖 record/idempotency/audit，但不覆盖 GSVA 分配/映射状态（provider 内
OBMM import mapping 是进程态）。

待澄清：重启后枚举 kernel segment 与 provider 有效状态的查询能力，现有 GSVA query/内核
proc 接口是否足够？若不足，属于计划允许的"确有 ABI 缺口才修改 kernel"——需要在 M1 阶段
先做缺口清单确认，避免 M3 时才发现要动 kernel/QEMU。

### D 类：流程与授权

#### D1. 提交授权范围

计划 §7：mem_service 先在子仓库形成干净提交、获授权发布后同步 gitlink + `mem_service.lock`；
commit/push 均按当次授权执行。

待澄清：

- 实现期是否授权直接在 mem_service 子仓库 commit 到 master 并 push 镜像？
- 主仓库 gitlink + lock 的同步更新是否随每笔 mem_service 发布同批授权？
- kernel/QEMU 若确需修改，push 镜像的对象仓库、分支与时机？

#### D2. sim-console 集成

计划 §6 说沿用 sim-console 目标配置与已有 launcher。

待澄清：M1 新用例是否需要接入 sim-console 的 run 类型/前端入口，还是仅 shell launcher +
报告文件即可（console 展示后续再补）？

#### D3. M1 内部提交节奏与最小验证集

计划要求每笔提交形成可测试的小能力、连续两次无改善即暂停。

待澄清：M1 是否接受按"服务 allocate/map 事务 → 两客户端 OBMM 共享 → UB-SSD 同服务用例 →
幂等/失败矩阵"切 3-4 笔提交？每笔提交对应的最小远端验证命令集是什么（避免每笔都跑
完整两节点矩阵导致周期过长）？

### 第一轮建议的开工顺序

1. 先回答 A1–A3、B1–B7（其中 B1、B2、B3、B5 是硬阻塞）；
2. 同步推进环境恢复（A1，可与澄清并行）；
3. 提交 docs 基线批次（A3）；
4. 按澄清结果补一节"M1 接口与部署决策"到计划文档，再开始编码。
